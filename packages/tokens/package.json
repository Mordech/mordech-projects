{
  "name": "@mordech/tokens",
  "version": "0.12.0",
  "prepare": "yarn run build",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./css": "./dist/css/index.css"
  },
  "scripts": {
    "build:ts": "tsc -b",
    "build:assets": "ts-node scripts/index.ts && prettier --write src/**/*.css",
    "build:css": "esbuild --bundle --minify --outfile=dist/css/index.css src/lib/styles/index.css --loader:.woff2=dataurl",
    "build:lib": "yarn build:ts && yarn build:assets && yarn build:css",
    "build": "yarn build:lib",
    "build:watch": "yarn build && nodemon --watch src/lib -i src/lib/styles/tokens.css --ext ts,css --exec \"yarn build:lib\" &",
    "test": "echo 'implement test'",
    "postinstall": "yarn build:assets",
    "lint": "eslint src"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1"
  },
  "nx": {}
}